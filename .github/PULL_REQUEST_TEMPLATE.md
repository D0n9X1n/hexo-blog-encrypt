# PR

Issue Fixed #

## Proposed Changes

- _
- _
- _

## Checklist

- [ ] Ran `npm run test` locally and it passed
- [ ] If adding a theme, included all 11 `{{hbe...}}` placeholders and 7 `data-*` attrs (see `docs/THEMES.md`)
- [ ] Kept `src/server/crypto.js` and `src/browser/crypto.js` wire-format compatibility; changed both halves and the format version together if needed

