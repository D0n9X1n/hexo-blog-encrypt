# PR

Issue Fixed #

## Proposed Changes

- _
- _
- _

## Checklist

- [ ] Ran `npm run test` locally and it passed
- [ ] If adding a theme, included all 7 `{{hbe...}}` placeholders and 5 `data-*` attrs (see `docs/THEMES.md`)
- [ ] Did not modify `index.js` or `lib/hbe.js` server↔browser crypto without updating both halves

